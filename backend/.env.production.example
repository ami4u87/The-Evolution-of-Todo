# Production Environment Configuration
# =====================================
# Copy this file to .env for production deployment

# Database Configuration (Neon Serverless PostgreSQL)
# Get your connection string from: https://console.neon.tech
# IMPORTANT: Include ?sslmode=require for Neon connections
DATABASE_URL=postgresql://neondb_owner:your-password@ep-xxx-xxx-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Authentication
# IMPORTANT: Use a strong, unique secret (minimum 32 characters)
# Generate with: openssl rand -base64 32
# This MUST match the frontend BETTER_AUTH_SECRET
BETTER_AUTH_SECRET=your-production-secret-key-minimum-32-characters-change-this

# JWT Configuration
JWT_ALGORITHM=HS256

# API Configuration
API_PREFIX=/api
DEBUG=false

# CORS Origins (comma-separated or JSON array)
# Add your production frontend URL(s)
CORS_ORIGINS=["https://your-production-domain.com"]

# AI Chat Configuration (Optional - Phase III)
# Choose one provider:

# Option 1: Groq (FREE) - Get key at https://console.groq.com/keys
AI_PROVIDER=groq
GROQ_API_KEY=your-groq-api-key
GROQ_MODEL=llama-3.3-70b-versatile

# Option 2: OpenAI - Get key at https://platform.openai.com/api-keys
# AI_PROVIDER=openai
# OPENAI_API_KEY=your-openai-api-key
# OPENAI_MODEL=gpt-4o-mini
