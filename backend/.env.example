# Database Configuration
# =====================
# Choose ONE of the following database options:

# Option 1: Neon Serverless PostgreSQL (RECOMMENDED for production)
# Get your connection string from: https://console.neon.tech
# Format: postgresql://<user>:<password>@<endpoint-id>.neon.tech/<database>?sslmode=require
DATABASE_URL=postgresql://neondb_owner:your-password@ep-xxx-xxx-123456.us-east-2.aws.neon.tech/neondb?sslmode=require

# Option 2: Local PostgreSQL (for development with Docker)
# DATABASE_URL=postgresql://todo_user:todo_password@localhost:5432/todo_db

# Option 3: SQLite (for quick local testing only - NOT for production)
# DATABASE_URL=sqlite:///./test_todo.db

# Authentication
BETTER_AUTH_SECRET=your-shared-secret-key-min-32-chars

# API Configuration
API_PREFIX=/api
DEBUG=true

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000
